# Internal MQTT listener — private Docker network only.
# No TLS, no authentication. Accessible only to trusted containers.
# Port 1883 is NOT published to the Docker host.
listener 1883
protocol mqtt
allow_anonymous true
