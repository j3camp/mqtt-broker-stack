---
services:
  mqttx-web:
    image: emqx/mqttx-web:latest
    restart: unless-stopped
    ports:
      - "${MQTTX_BIND_ADDRESS:-127.0.0.1}:${MQTTX_PORT:-80}:80"
    networks:
      - mqtt-internal

networks:
  mqtt-internal:
